{
  "name": "orpington-news",
  "private": true,
  "scripts": {
    "prepare": "husky install",
    "dev": "cross-env NODE_ENV=development lerna run dev --stream --parallel",
    "test": "lerna run test --stream --parallel",
    "build": "lerna run build --stream"
  },
  "devDependencies": {
    "cpy-cli": "^3.1.1",
    "cross-env": "^7.0.3",
    "husky": ">=6",
    "lerna": "^4.0.0",
    "lint-staged": ">=12.3.3",
    "prettier": "^2.5.1",
    "rimraf": "^3.0.2",
    "typescript": "^4.5.5"
  },
  "workspaces": [
    "packages/*"
  ],
  "lint-staged": {
    "*.{js,css,md,ts,tsx,json,yml,yaml}": "prettier --write"
  }
}
